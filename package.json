{
  "name": "grabtern.com",
  "version": "1.0.0",
  "description": "This is website for providing the mentorship program",
  "main": "index.js",
  "scripts": {
    "nstart": "concurrently --kill-others \"cd frontend && npm install && npm run dev\" \"cd backend && npm install && npm run start\"",
    "ystart": "concurrently --kill-others \"cd frontend && yarn install && yarn run dev\" \"cd backend && yarn install && yarn run start\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/anmode/Move-to-nextJS.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/anmode/Move-to-nextJS/issues"
  },
  "homepage": "https://github.com/anmode/Move-to-nextJS#readme",
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
